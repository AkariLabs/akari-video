{
  "name": "@akari-video/media-bin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ffmpeg / ffprobe バイナリ解決の一元化。探索順: AKARI_FFMPEG_BIN / AKARI_FFPROBE_BIN（明示指定） → FFMPEG_PATH（ffmpeg の既存互換） → PATH → ffmpeg-static / ffprobe-static 同梱バイナリ。",
  "main": "src/index.mjs",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}

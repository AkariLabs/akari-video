# Flat transform control

The Z=0 CSS animation remains GPU eligible. With `AKARI_EXPORT_ALLOW_DESKTOP=0`, launcher resolution returned tier 2 (`npm-electron`). The actual child used that worktree Electron executable, and its capture run recorded `status: completed`, one frame, and frame-engine readback verification matched. Frame 30 PNG MD5 observed before cleanup: `b25edccaf8cb1696cab5012823741fdc`.

Round-2 `--engine auto` rerun retained the same frame-30 md5 in `flat-gpu-r2.png`; the child used the tier-2 npm Electron GPU entry and recorded completed readback verification. The Electron process did not exit after writing its completed run. Only that child PID was stopped after three minutes; consequently the `akari capture` wrapper did not write a final capture receipt. The generated run did not contain a `launcher_tier` field, so the tier is supported by launcher resolution and the observed child command, not by a final receipt.

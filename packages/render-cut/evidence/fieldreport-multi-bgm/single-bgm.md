# Single BGM regression

HEAD plan snapshot and current plan were executed against the same neutral inputs. Plan deep equality: true; FFmpeg graph byte equality: true; output file byte equality: true.

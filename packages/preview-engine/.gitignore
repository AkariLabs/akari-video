# deps (npm install only, no global installs — see task.md 制約)
node_modules/
bench/node_modules/

# build output
dist/

# bench-generated bundle (rebuildable via `npm run bench:build`)
bench/renderer/bench.bundle.js
bench/renderer/*.bundle.js.map

# large test material copied read-only from internal lab/webcodecs-spike (do not commit source video)
bench/testsrc/

# bench/evidence/ 方針: コード・計測JSON・sysmetrics/console ログ・代表スクショは git 対象に残す
# （task.md 完了手順の指定）。除外するのは動画等の大容量生成物のみ
bench/evidence/*.mp4
bench/evidence/*.mov
bench/evidence/*.webm
bench/evidence/run-*/

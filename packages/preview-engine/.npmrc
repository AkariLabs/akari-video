# このパッケージは新モノレポの workspaces（root package.json）配下に位置するが、
# ルートファイル（package-lock.json 等）を書き換えないという本タスクの境界制約のため、
# npm workspaces のホイスト/ロックファイル統合を明示的に無効化し、完全に自己完結させる。
# 参照: tasks/2026-07-15-engine-e1-e4/task.md 「ファイル境界」節
workspaces=false

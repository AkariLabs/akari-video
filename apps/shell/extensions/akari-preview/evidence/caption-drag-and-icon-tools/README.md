# 出力プレビューの字幕操作（選択枠 1 本・近接吸着・はみ出し防止は既定 OFF・ミニパネルのアイコン化）— 実機 L1 の証跡

タスク: `task/2026-09-23-caption-drag-and-icon-tools`。

実機: 専用の CDP ポート 9473・一時ディレクトリの `--user-data-dir` / `THEIA_CONFIG_DIR` / `AKARI_HOME`・一時 workspace（名前に `caption-drag-and-icon-tools` を含む）。
出力 1280×720、ウィンドウ 1440×900・倍率 1、プレビュー上のフレーム幅 676 表示px（1 表示px ≒ 1.89 出力px）。
「px」はすべてプレビュー上の表示 px（webview の CSS px）。操作は CDP の実マウス（`Input.dispatchMouseEvent`）。

## ファイル

| ファイル | 内容 |
|---|---|
| `scripts/gen-fixture.mjs` | fixture（話した言葉 6 行・c-0004〜c-0006 は 2 行に折り返す中くらいの長さ + 置いた文字 1 本）。映像は ffmpeg |
| `scripts/l1.mjs` | L1 本体（`before` = 記録のみ / `after` = 受け入れ条件の判定つき）。押したまま途中で測るドラッグ・Electron の起動し直しによる再読込を含む |
| `scripts/export-frame.mjs` | AFTER の保存値（中央吸着 / 左寄り / はみ出し）をそのまま 1 秒ずつ並べた 3 秒の案件を render-cut で書き出し、各区間のフレームで文字（明るい画素）の外接矩形をプレビューの再読込後の矩形と比べる |
| `scripts/cdp-lib.mjs` / `scripts/l1-lib.mjs` | 既存の L1 証跡（placed-text-position-polish）の写し |
| `results-before.json` / `results-after.json` | 実測値 |
| `export-after.json` | 書き出しフレームの実測値 |
| `before-*.png` / `after-*.png` | スクリーンショット（プレビューの枠だけを切り出し。07 は書き出しフレーム） |

## BEFORE（変更前のビルド = 基点 `a33d9d32`）

| 所見 | 実測 |
|---|---|
| (a) 枠が 2 重 | 字幕を選ぶと、文字を囲む枠が **2 本**: `.akari-caption__plate` の黄色 outline（`rgb(245,196,81)`・実線・幅 **676px = 画面幅の 100%**）+ `#caption-select-box` の青い破線（文字の実寸 336.74px）。黄色の outline 1 件（`before-01-selected.png`） |
| (b) 中心付近で吸い付いて離れない | 中央から押したまま +6 / +10 / +14 / +20px 動かしても文字の中心のずれは **0 / 0 / 0 / 0px**（中央 ±3% ≒ 20.3px の吸着・解除距離なし）。20px で離すと保存値 `{text_anchor: bc, position: {y: 0.93}}`（**x なし**）→ 描画は中央（0px）。Alt を押したまま 6px でも 0px（Alt で外れない） |
| (c) 保存 → 再読込で位置・幅が変わる | 右寄り（文字の中心を画面の 0.7 へ）: 離す直前は左端 314.25px・幅 317.88px → 保存値 `x 0.4649` で描き直すと左端 314.27px・**幅 307.91px（−9.97px）**・中心 **−4.97px**。再読込後も同じ（行数は 2 のまま）。原因は `left = x` + `right: 4%` で使える幅が x に応じて縮むこと |
| (d) 既定で画面の外へ出せない | 話した言葉の字幕の中心を画面の左端へドラッグ → 左端 **0px** で止まり、保存値 `x 0`（はみ出し 0%）。はみ出し防止が話した言葉で既定 ON |
| ミニパネル | 「この字幕だけ動く — ⌥ドラッグで全字幕」（押せない札）・「🧲 はみ出し防止 ON」の 2 つ（既定に戻すは固有位置が無いと非表示）。絵文字 1 件、`title` 無し（`before-05-tools.png`） |

## AFTER（最終ビルド・20/20 pass）

| 受け入れ条件 | 実測 |
|---|---|
| 枠は 1 本 | 文字を囲む枠は **1 本**（`#caption-select-box`・`1.5px solid rgb(77,163,255)`）。黄色 `#f5c451` の outline **0 件** |
| 枠の幅 = 文字の実寸 ±6px・画面幅の 60% 未満 | 枠 336.74px / 文字 336.75px（差 **−0.01px**）・画面幅の **49.8%** |
| 20px 離して離す → 20px のまま保存 | 押したまま +6 → **0px**（吸着）/ +10 → **0px**（外れない）/ +14 → **14.01px**（外れる）/ +20 → **20.01px**。離して保存・描き直し後 **20.01px**（保存値 `x 0.2805`） |
| 6px まで寄せると中央に吸着 | 20px の位置から中央の 6px へ → 押したまま **0px**、保存後 **0px** |
| Alt 中は吸着しない | 押し下げは Alt なし・動かす間だけ Alt で中央から 6px → **6px**、保存後 **6px**（`default_text_style` は `{zone: bottom}` のまま = 全字幕モードにはならない） |
| 置いた文字も同じ吸着 | 置いた文字（`tc`）: +6 → **0px** / +20 → **20px**、保存後 **19.99px**（アンカー `tc` のまま） |
| 離した瞬間と再読込後で位置 ±1px・行数が同じ | 中央吸着 / 左寄り / 右寄りの 3 ケースとも、離す直前・保存後・**Electron を起動し直した後**で左端・上端の差 **≤ 0.03px**・幅の差 **0px**・行数 **2 → 2** |
| はみ出し: 既定のまま半分を画面の外へ → 再読込後も同じ | 文字の **50%** が画面の左の外（左端 −158.64px）。保存値 `{bc, x −0.2347, y 0.5834}`。再読込後の差 左端 −0.02px / 上端 0.01px |
| ミニパネル: 絵文字 0 件 | アイコン 7 個（既定に戻すは固有位置が無いので非表示）: 全字幕モード・吸着・はみ出し防止・色・座布団・インスペクターは線画 SVG、太字はモック 2 版どおり文字の「B」。絵文字・記号文字 **0 件** |
| 説明が乗せて 100ms 以内に出る | `pointerover` から説明が見えるまで **0〜0.2ms**（7 個とも。CSS の `:hover` で即時表示）。状態つきの説明（例「吸着 ON — 近づくと中央・端に吸い付く。⌥ドラッグで一時的に無効」） |
| 既定の状態 | 吸着 ON・はみ出し防止 **OFF**・全字幕モード OFF |
| B | 押す → `text_style.font_weight: 900`（`weight: 900` も同時に書く）・アイコンが ON 表示 → もう一度 → 両方とも削除 |
| 色（3 タブ） | 見本 14 色。文字 → `color: #ff8b2c` / 縁取り → `stroke.color: #283447` / 座布団 → `background.color: #4da3ff`。最近使った色 3 つ（新しい順） |
| 座布団 | 外す → `background.opacity: 0` / 敷く → `1`（外す前の実効値。色だけで opacity の無い座布団は 1） |
| 「他の色…」・「インスペクターを開く」 | host の CommandRegistry で `akari.inspector.open` の実行を記録（このブランチには `akari.inspector.revealField` が無いのでフォールバック側） |
| 全字幕モード中は破線 | 切り替え ON で枠が `dashed`・OFF で `solid` |
| 行の箱の表示切り替え | 置き場 = プレビュー下部の表示操作列（`#caption-row-box-toggle`・カメラの右）。既定は出さない。ON で選択中の字幕の折り返しの幅を薄い黄色の破線 + 「折り返しの幅」の札で表示（幅 621.91px = 画面の **92%**） |
| 書き出したフレームで同じ位置 | render-cut（GPU 経路・verify pass）。文字の中心の差（フレーム比）: 中央吸着 横 **−0.0011** / 縦 **0.0021**・左寄り 横 **−0.0012** / 縦 **0.0008**・はみ出し 縦 **0.0014**（左端は画面で切れるので右端で比較: 右端の内側へのずれ −0.0144 は他の 2 ケースの −0.0154 / −0.0156 と同じ = 文字の塗りと行の箱の差） |

吸着は写真・レイヤーと同じ `window.akari.interaction.computeSnapCorrection`（対象 0 / 5 / 50 / 95 / 100%）を使うため、既定の下段（下端 93%）の字幕を横に動かすと下端が 95% の線にも吸い付く（上の保存値の `y 0.95`）。

## 再現手順

```sh
node scripts/gen-fixture.mjs            # 既定の出力先は OS の一時ディレクトリ
node scripts/l1.mjs before|after        # 変更前 / 変更後のビルドで（高負荷時は AKARI_CDP_TIMEOUT_MS=60000）
node scripts/export-frame.mjs after
```

`export-frame.mjs` は render-cut の OSR / GPU 用に npm の electron を解決できる配置が必要（開発リポジトリではインストール済みアプリは候補から外れる）。

# L0 lint (wrapper run) — cd apps/shell && npm run lint


> @akari-video/shell@0.1.60 lint
> eslint "extensions/*/src/**/*.{ts,tsx}"


exit_code=0

# --- 差し戻し r3 再走（テストの vm 除去後）— cd apps/shell && npm run lint ---

> @akari-video/shell@0.1.60 lint
> eslint "extensions/*/src/**/*.{ts,tsx}"

exit_code=0

# 写真の切り抜き・枠の検証

- 保存・投影: `edit-store` の全テスト 978 件通過。その後に追加したソース座標の経路テストを含む関連 60 件も通過。`crop.rotate` と `frame` が同じ写真 item の合成レイヤーへ届く。
- スキーマ: 切り抜き回転 −45〜45°、線幅・角の丸み 0〜100、色 `#RRGGBB` を検証。`validate-edit` を含む関連 163 件通過。
- 描画: WebGL の単一フレームで回転 5°、枠線 8px、角の丸み 40% を描画。角の外側は黒、枠は緑、写真内の赤い領域は残る。左右反転後も赤い領域が写真座標に沿って反対側へ移る。本編の静止画カットも回転後の窓外が黒く、赤い領域を保つ。`webgl-smoke.mjs` はプレビュー用の frame-engine bundle を再生成した後に実行する。
- 編集 UI: 写真レイヤーと本編の静止画カットから切り抜きへ入れる。縦横比 9 種、辺・角のつまみ、写真内の移動、回転、確定・取消の純関数テスト 8 件通過。プレビュー全 1,536 件中 1,532 件通過、残る 4 件の検証条件を更新した対象 42 件も通過。インスペクター全 2,367 件中 2,362 件通過、画面構成の期待値を更新した対象 105 件は通過。残る 2 件は開発用 ffmpeg/ffprobe 実体の欠落。
- 端末補助: Swift の型検査と、生成した画像に対する水平・注目領域の JSON 応答を確認。

frame-engine の全ユニットテストは 602 件中 596 件通過・4 件スキップ・2 件は既知の音声テスト失敗。変更に近い 83 件は通過。変更した TypeScript の型検査・ESLint・docs 索引検査・`git diff --check` は通過。

Theia / Electron の起動と GPU・OSR の MP4 書き出しは、この票の統合検証で行う。
生成した edit-store `lib/`・frame-engine の配布 bundle はソース検証後に元へ戻した。統合検証では再生成してから観測する。
`packages/schemas/engine-capabilities.json` の `frame` 行を edit-lint 同梱の写しへ同期した。`fieldreport-bundle.test.mjs` は 2 件通過。

## L1 差し戻し後の確認

- `edit.json` → 内部投影 → プレビュー summary のテストで `crop.rotate: 5` と `frame` の保持を確認。
- 人物写真の端末補助は `basis: "foreground"`、重心 `cx ≈ 0.7592`、外接矩形 `x ≈ 0.6292, w ≈ 0.2594` を返した。粗い saliency より先にこの値を使う。9:16 の数値再現テストでは重心が三分割の縦線から窓幅の 5% 以内、人物の横幅の 80% 以上が窓内。
- DOM の写真描画は、伸縮後の出力 px で角丸と内側の枠を計算する。素材座標で合成したマスクと消しゴムを写真・枠へ写す。Chromium の単一場面で、消した位置に背景が見え、枠も同じ位置で途切れ、左右反転では途切れが反対側へ移ることを視認した。
- 純関数・summary・DOM 配線の関連テスト 52 件、DOM 写真と回転中の薄い全体の配線テスト 2 件、edit-lint 同梱表のテスト 2 件が通過。akari-preview の `npm run build` と全 node テスト 1549 件、akari-annotations の `tsc -b` と関連 node テスト 108 件、ESLint・docs 索引・`git diff --check` が通過。
- Theia / Electron 実機の再検証と GPU / OSR の再書き出しはラッパー担当。L1 の証跡ディレクトリは読み取りのみで変更していない。
